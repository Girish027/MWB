/* eslint-disable */
let config = {
    "env": process && process.env && process.env.NODE_ENV ? process.env.NODE_ENV : "production",
    "lang": process && process.env && process.env.lang ? process.env.lang : "english"
};

export default config;
