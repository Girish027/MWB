
import ProjectsManager from 'model/ProjectsManager/ProjectsManager';
import * as ModelConfigManager from 'model/ModelConfigManager';

export const Model = {
  ProjectsManager,
  ModelConfigManager,
};

export default Model;
