/*import Constants from 'constants/Constants';
const amplitude = jest.genMockFromModule('amplitude-js/amplitude.nocompat');

const MOCK_FUNC_OBJECT = {
  init: jest.fn((amplitudeApiKey, name) => 'called amplitude init'),
  logEvent: jest.fn((eventName, eventProperties = Constants.OBJECT_DEFAULT_VALUE) => 'called amplitude logEvent')
};

amplitude.getInstance = jest.fn(() => MOCK_FUNC_OBJECT);

export default amplitude;*/
